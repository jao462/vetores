#include <iostream>
using namespace std;

int main() {
    int N;
    cin >> N;

    int vet[10];

    for (int i = 0; i < N; i++) {
        cin >> vet[i];
    }

    for (int i = 0; i < N; i++) {
        if (vet[i] < 0) {
            cout << vet[i] << " ";
        }
    }

    return 0;
}
